# Porta (Railway injeta PORT em produção)
PORT=4000

# MySQL (preencher no Railway, não commitar .env real)
DB_HOST=mysql.railway.internal
DB_PORT=3306
DB_USER=root
DB_PASSWORD=COLOQUE_NO_RAILWAY
DB_NAME=railway

# Sessão/JWT
JWT_SECRET=COLOQUE_NO_RAILWAY
JWT_EXPIRES=1d

# CORS (origens permitidas: separar por vírgula)
FRONTEND_ORIGINS=http://localhost:5173,https://SEU-FRONT.vercel.app

# Cookies (produção: secure=true, samesite=none)
COOKIE_SECURE=false
COOKIE_SAMESITE=lax
